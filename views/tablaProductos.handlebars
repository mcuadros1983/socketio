<div class="container" style="background: rgb(174, 203, 214);">
    <div class="jumbotron" style="height:100vh">
        <h1 style="color:blue">VISTA DE PRODUCTOS</h1>
        {{#if data}}
        <div class="table-responsive">
            <table class="table table-dark" style="text-align: center;">
                <tr style="color:white;">
                    <th>NOMBRE</th>
                    <th>PRECIO</th>
                    <th>IMAGEN</th>
                </tr>
                {{#each data}}
                <tr>
                    <td>
                        {{this.title}}
                    </td>
                    <td>
                        {{this.price}}
                    </td>
                    <td>
                        <img src="{{this.image}}" alt="" width="50px" height="50px">
                    </td>
                </tr>
                {{/each}}
            </table>
        </div>
        {{else}}
        <div style="background: rgb(240, 240, 178); color:rgb(141, 143, 23)">
            <p style="font-size: x-large;">No se encontraron productos</p>
        </div>
        {{/if}}
        {{!-- <a href="/"><button type="button" class="btn btn-secondary">Volver al Formulario</button></a> --}}
    </div>
    
</div>
